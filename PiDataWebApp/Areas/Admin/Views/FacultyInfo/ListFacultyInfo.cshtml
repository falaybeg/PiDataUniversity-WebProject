@model IEnumerable<PiDataWebApp.Models.FacultyInfoViewModel>


<div class="row">
    <div class="panel panel-default panel-table">
        <div class="panel-heading">
            <center>
                <h2 class="page-head-title"><b><b>Fakülte Tanıtım Yönetim Ekranı</b></b></h2>
            </center>
            <br /><br />
            <div class="col-sm-4">
                <div><a href="@Url.Action("AddFacultyInfo","FacultyInfo", new { Area = "Admin"})"><button class="btn btn-success">Yeni Fakülte Tanıtım</button></a></div>
                <br />
            </div>

            <div class="panel-body">
                <table class="table table-striped table-hover" id="productTablee">
                    <thead>
                        <tr style="font-size: 16px;  background-color:lavender">
                            <th width="20%">Fakülte Adı</th>
                            <th width="23%">Adres</th>
                            <th width="11%">Telefon</th>
                            <th width="11%">Fax No</th>
                            <th width="15%">Email</th>
                            <th width="15%">Üniversite</th>
                            <th> </th>
                        </tr>
                    </thead>
                    <tbody>
                             @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Name</td>
                                    <td>@item.Address</td>
                                    <td>@item.Phone</td>
                                    <td>@item.FaxNo</td>
                                    <td>@item.Email</td>
                                    <td>@item.UniversityInfo.Name</td>
                                    <td class="text-right">
                                        <div class="btn-group btn-hspace">
                                            <button type="button" data-toggle="dropdown" class="btn btn-default dropdown-toggle " aria-expanded="false">Action <span class="icon-dropdown mdi mdi-chevron-down"></span></button>
                                            <ul role="menu" class="dropdown-menu pull-right">
                                                <li><a href="@Url.Action("EditInfo","FacultyInfo", new { Area = "Admin", id = item.Id})"><i class="icon mdi mdi-edit"></i><span>Edit</span></a></li>
                                                <li><a href="#" onclick="Delete(@item.Id)"><i class="icon mdi mdi-delete"></i><span>Delete</span></a></li>
                                            </ul>
                                        </div>
                                    </td>
                                </tr>
                            }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

    <script>
     var token = sessionStorage.getItem("accessToken");
        var headers = {};
        headers.Authorization = "Bearer " + token;

        $(document).ready(function () {
            if (token == null) {
                window.location.href = "/Admin/UserManagement/Login";
            }
        });

        var Delete = function (id) {

            $.ajax({
                url: "/api/FacultyInfo/" + id,
                method: "DELETE",
                headers: headers,
                success: function () {
                    alert("Faculty deleted successfully");
                    window.location.replace("/Admin/FacultyInfo/ListFacultyInfo");

                },
                error: function (jqXHR) {
                    alert("Error !");
                }
            });
        };

    </script>




